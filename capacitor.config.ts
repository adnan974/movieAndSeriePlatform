import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-projet-dossier-professionnel',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
